import React from 'react'
import { connect } from 'react-redux'

export const List = () => {
  return (
    <div>
      
    </div>
  )
}

const ListToShow = (state) => {
  
}

const mapStateToProps = (state) => {
  return {

  }
}